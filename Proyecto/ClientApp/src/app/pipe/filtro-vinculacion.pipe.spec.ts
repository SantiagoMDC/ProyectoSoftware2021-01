import { FiltroVinculacionPipe } from './filtro-vinculacion.pipe';

describe('FiltroVinculacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVinculacionPipe();
    expect(pipe).toBeTruthy();
  });
});
